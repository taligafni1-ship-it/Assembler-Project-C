START: mov r1 r2 
add r1, r2, 
add r1, r2
sub r1,, r2 

cmp r1, 

mov , r2 

LABEL1 inc r1 

: mov r1, r2 

mov r1, #5 
add r2, #10 
sub r3, #-7 
inc #5 
clr #0 

lea #5, r1 
le1: mov r1, r2 
lea r1, r2 

jmp #100 
bne r1 
 

stop r1 
rts r2, r3 
inc r1, r2 
clr r1, r2, r3 

mov r1 
add r2 
jsr #50
cmp 


mov 
lea 
add 
sub 
stop r1, r2 
rts LABEL 

mvo r1, r2 
addd r1, r2 
jmpp LABEL 
halt 


mov r8, r1 
add r1, r9 
sub r-1, r2 
inc r10 

sub r1a, r2 

mov #, r1 

mov #5a, r1 
add #12.5, r2 
sub ##5, r3 
cmp #, r1
